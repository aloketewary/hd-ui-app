import { UserCdkDatasource } from './user-cdk-datasource';

describe('UserCdkDatasource', () => {
  it('should create an instance', () => {
    expect(new UserCdkDatasource()).toBeTruthy();
  });
});
